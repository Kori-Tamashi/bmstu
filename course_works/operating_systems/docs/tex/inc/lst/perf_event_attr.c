struct perf_event_attr {
    __u32 type;                 // Тип события (аппаратное, программное, tracepoint)
    __u32 size;                 // Размер структуры (для обратной совместимости)
    __u64 config;               // Специфическая конфигурация события
    
    // Битовые поля флагов:
    __u64 sample_period;        // Период сэмплирования
    __u64 sample_type;          // Тип данных в сэмплах
    __u64 read_format;          // Формат данных при чтении
    __u64 disabled : 1,         // Начинать в выключенном состоянии
          inherit : 1,          // Наследование дочерним процессам
          pinned : 1,           // Закрепление на CPU
          exclusive : 1,        // Эксклюзивный доступ к PMU
          exclude_user : 1,     // Исключить пользовательский режим
          exclude_kernel : 1,   // Исключить режим ядра
          exclude_hv : 1,       // Исключить гипервизор
          exclude_idle : 1,     // Исключить время простоя
          // ... другие флаги
    __u64 wakeup_events;        // Количество событий для wakeup
    __u32 bp_type;              // Тип точки останова
    __u64 bp_addr;              // Адрес точки останова
    __u64 bp_len;               // Длина точки останова
    // ... другие поля
};